<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registel</title>

  <%- include('@headerlinks') %>


  <script src="<%-  
      [
          "/", 
          info_pagina.carpeta.replace( info_pagina.nombre, "" ) , 
          info_pagina.nombre.replace( info_pagina.carpeta, "" ) 
      ].filter(Boolean)
      .join("/")
      .replaceAll("///", "/")
      .replaceAll("//", "/")  
  %>.jsx" type="text/babel" defer></script>

  <script src="/JSX/!fin.jsx" type="text/babel" defer></script>
</head>

<body>
</body>

</html>